/**
 * Selects a random background image URL from the local assets.
 * @returns {string} The path to a random background image.
 */
export const getRandomBackgroundImage = (): string => {
    return ''; // Return empty string as images are no longer used.
};